[package]
name    = "abalone"
version = "0.1.0"
edition = "2024"

[lib]
name    = "abalone"
path    = "src/lib.rs"
test    = false
doctest = false

[dependencies]
bitwise   = { path = "../bitwise" }
spin      = { version = "0.9", default-features = false, features = ["mutex", "spin_mutex"] }
